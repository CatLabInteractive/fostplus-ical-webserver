# Consumer key for the Fostplus API.
# This key is required to authenticate with the API.
# You can find it by inspecting the network requests on https://www.fostplus.be/
# or by checking the fostplus-api-wrapper project tests.
FOSTPLUS_CONSUMER_KEY=your_consumer_key_here

# Port for the web server (default: 3000)
PORT=3000
